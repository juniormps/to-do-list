import styles from "./Footer.module.css";

const Footer = () => {
    return (
        <footer className={styles.footer}>
            <p>
                <span>Desenvolvido por Marcio Junior</span> @2026
            </p>
        </footer>
    );
};

export default Footer;
